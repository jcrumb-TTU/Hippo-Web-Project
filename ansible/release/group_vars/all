# srcs: The paths of resources relative to toplevel, which is in the tmpdir. Everything after toplevel is relative to the repository.
repo_toplevel: "/Hippo-Web-Project-Deployment"
srcs:
  toplevel: "{{repo_toplevel}}"
  tarball: "{{repo_toplevel}}.tar.gz"
  html: "{{repo_toplevel}}/srv/html"
  HippoAPI: "{{repo_toplevel}}/Hippo-Exchange"
  HippoAPIPub: "{{repo_toplevel}}/Hippo-Exchange/bin/Release/net8.0/publish/"
  patches: "{{repo_toplevel}}/submodules/module-patches"
  swagger_ui: "{{repo_toplevel}}/submodules/swagger-ui"
  swagger_spec: "{{repo_toplevel}}/srv/dev"
  swagger_validator: "{{repo_toplevel}}/submodules/swagger-validator"
  nginx: "{{repo_toplevel}}/config/nginx"
  dotnet: "~/.dotnet"

# dests: The paths resources are installed to on the system.
dests:
  dotnet: "/usr/share/dotnet"
  HippoAPI: "/usr/local/share/HippoAPI"
  nginx: "/etc/nginx/"
  html: "/srv/html"
  swagger_validator: "/usr/local/share/swagger-validator"
  swagger_spec: "/srv/dev"
  swagger_ui: "/srv/dev/swagger"

# Dotnet install information.
dotnet_url: 'https://dot.net/v1/dotnet-install.sh'
dotnet_path: "~/.dotnet"

